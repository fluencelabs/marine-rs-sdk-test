modules_dir = "artifacts/"
total_memory_limit = "Infinity"

[[module]]
    name = "greeting"
    mem_pages_count = 50
    logger_enabled = false
    [module.mounted_binaries]
        echo = "/usr/bin/curl"
